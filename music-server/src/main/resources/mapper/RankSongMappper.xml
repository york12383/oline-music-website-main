<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.music.mapper.RankSongMapper">
    <resultMap id="BaseResultMap" type="com.example.music.model.domain.RankSong">
        <result column="id" property="id" jdbcType="BIGINT" />
        <result column="songId" property="songId" jdbcType="INTEGER" />
        <result column="consumerId" property="consumerId" jdbcType="INTEGER" />
        <result column="score" property="score" jdbcType="INTEGER" />
    </resultMap>

    <select id="selectScoreSum" resultType="java.lang.Integer">
    SELECT COALESCE(sum(score), 0) as score from rank_song where song_id = #{0} ;
    </select>

    <select id="selectUserRankSong" resultType="java.lang.Integer" parameterType="com.example.music.model.domain.RankSong">
        select score
        from rank_song
        where consumer_id = #{consumer_id, jdbcType=INTEGER} and song_id = #{song_id, jdbcType=INTEGER}
    </select>



</mapper>